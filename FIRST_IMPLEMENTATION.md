# First Implementation: Блочный редактор документов 🚀

## Vision: Что мы строим

**SimpleDoc v1.0** — это первый шаг к революции в создании бизнес-документов. Мы создаём визуальный редактор, который объединяет гибкость Notion с точностью Canva, специально для профессиональных документов.

## 🎯 Цель первой реализации

Создать **работающий прототип блочного редактора**, где пользователь может:
- Зайти в приложение и увидеть разные страницы
- Увидеть вкладку с реальной страницей A4 на холсте
- Добавлять блоки (текст, изображения, таблицы) через drag-and-drop
- Редактировать блоки напрямую
- Генерировать идеальный PDF с точным позиционированием (реализовано на Python)

**Результат:** Пользователь создаёт профессиональный документ за 5 минут вместо часа в Word.
**Результат:** Пользователь создаёт профессиональный документ за 5 минут вместо часа в Word.

## 🎨 Интерфейс: Как это выглядит

```
┌─────────────────────────────────────────────────────────────┐
│ Header: [Logo] SimpleDoc    [File] [Edit] [View] [Generate] │
├──────┬──────────────────────────────────────────────┬───────┤
│      │                                              │       │
│ Side │  ┌──────────────────────────────────────┐  │ Props │
│ Panel│  │  Canvas: A4 Page (210×297mm)         │  │ Panel │
│      │  │  ┌────────────────────────────────┐ │  │       │
│[Blocks│  │  │ [Text Block]                   │ │  │[Style]│
│  📝  │  │  │ Коммерческое предложение        │ │  │[Layout│
│  🖼️  │  │  └────────────────────────────────┘ │  │[Data] │
│  📊  │  │                                      │  │       │
│  ➕  │  │  ┌──────────┐  ┌──────────────┐   │  │       │
│      │  │  │ [Image]  │  │ [Table Block]│   │  │       │
│      │  │  └──────────┘  └──────────────┘   │  │       │
│      │  └──────────────────────────────────────┘  │       │
│      │                                              │       │
└──────┴──────────────────────────────────────────────┴───────┘
```

**Ключевые элементы:**
- **Side Panel (280px)** — библиотека блоков, шаблоны, ассеты
- **Canvas (центр)** — реальная страница A4 с сеткой, блоки с ручками
- **Properties Panel (320px)** — стили, позиция, данные выбранного блока

## 🏗️ Архитектура первой версии

### Frontend (Svelte)

**Компоненты:**
- `Canvas.svelte` — основной холст с реальной страницей A4
- `Block.svelte` — базовый компонент блока (текст, изображение, таблица)
- `BlockLibrary.svelte` — панель с доступными блоками
- `PropertiesPanel.svelte` — панель свойств выбранного блока
- `PageRuler.svelte` — линейки и направляющие

**Stores:**
- `blocks.ts` — массив блоков на странице
- `selectedBlock.ts` — текущий выбранный блок
- `page.ts` — настройки страницы (размер, поля, ориентация)

**Технологии:**
- Svelte 5 runes ($state, $derived, $effect)
- TailwindCSS по FORDESIGNER.md
- `svelte-dnd-action` для drag-and-drop
- Canvas API для отрисовки страницы и сетки

### Backend (Rust/Tauri)

**Commands:**
- `generate_pdf(blocks: Vec<Block>) -> Result<String>` — генерация PDF
- `save_document(document: Document) -> Result<()>` — сохранение документа
- `load_document(id: String) -> Result<Document>` — загрузка документа
- `execute_python_script(script: String, args: Vec<String>) -> Result<String>`

**Модели:**
- `Block` — структура блока (id, type, position, size, content, styles)
- `Document` — структура документа (id, pages, blocks, metadata)
- `Page` — настройки страницы (size, margins, orientation)

### Python Generator

**Модуль:** `pdf_generator.py`
- `generate_pdf(blocks: List[Dict], page_config: Dict) -> bytes`
- Использует `reportlab` для точного позиционирования
- Конвертирует блоки в PDF элементы с координатами

## 🎯 Функциональность v1.0

### ✅ Реализуем в первой версии

**1. Базовый Canvas**
- Реальная страница A4 (210×297 мм) на холсте
- Сетка 8px для выравнивания
- Масштабирование (zoom in/out)
- Прокрутка для длинных документов

**2. Типы блоков (базовые)**
- **Text Block** — заголовки, параграфы (inline редактирование)
- **Image Block** — загрузка изображений, изменение размера
- **Table Block** — простая таблица (редактируемые ячейки)
- **Spacer Block** — отступ между блоками

**3. Управление блоками**
- Добавление через drag-and-drop из Side Panel
- Выделение блока (клик, рамка выделения)
- Перемещение (drag блок)
- Изменение размера (drag ручки)
- Удаление (Delete key)

**4. Редактирование**
- Inline редактирование текстовых блоков
- Панель свойств для выбранного блока:
  - Позиция (X, Y)
  - Размер (ширина, высота)
  - Стили (шрифт, размер, цвет, выравнивание)
  - Для изображений: замена, обрезка

**5. Генерация PDF**
- Кнопка "Generate PDF"
- Конвертация всех блоков в PDF с точными координатами
- Сохранение файла
- Открытие сгенерированного PDF

**6. Сохранение/Загрузка**
- Сохранение документа в JSON (локально)
- Загрузка сохранённого документа
- Автосохранение каждые 30 секунд

### ⏭️ Откладываем на v2.0

- AI интеграция
- Множественные страницы
- Группировка блоков
- Условная логика
- Повторы (repeater)
- Анимации
- DOCX экспорт (только PDF в v1.0)

## 🎨 Дизайн: Следование FORDESIGNER.md

**Цвета:**
- Canvas background: `gray-100` (#f1f5f9)
- Page background: `white`
- Grid dots: `gray-300` (#cbd5e1)
- Block selection: `blue-500` (#0ea5e9) с рамкой 2px
- Side panels: `gray-50` (#f8fafc)

**Типографика:**
- UI текст: `Inter`, 14px (text-sm)
- Заголовки: `Inter`, 16-24px, semibold
- Блоки текста: `Inter`, 16px (text-base)

**Spacing:**
- Панели: padding 16px (spacing-4)
- Блоки: margin 8px между блоками
- Сетка: 8px шаг

**Анимации:**
- Hover: 150ms transition
- Drag: плавное перемещение
- Модальные окна: 300ms fade-in

## 🔧 Технические детали

### Структура данных блока

```typescript
interface Block {
  id: string;
  type: 'text' | 'image' | 'table' | 'spacer';
  position: { x: number; y: number }; // в пикселях от верхнего левого угла страницы
  size: { width: number; height: number };
  content: BlockContent;
  styles: BlockStyles;
  zIndex: number;
}

interface TextBlockContent {
  text: string;
  fontSize: number;
  fontFamily: string;
  fontWeight: number;
  color: string;
  alignment: 'left' | 'center' | 'right';
}

interface ImageBlockContent {
  src: string; // путь к файлу или base64
  alt: string;
}
```

### Генерация PDF (Python)

**Процесс:**
1. Frontend отправляет массив блоков в Tauri command
2. Rust сериализует в JSON и передаёт в Python скрипт
3. Python (reportlab) создаёт PDF:
   - Создаёт страницу A4 (210×297 мм)
   - Для каждого блока:
     - Конвертирует координаты (px → mm)
     - Рисует элемент на точной позиции
     - Применяет стили (шрифты, цвета, размеры)
4. Возвращает путь к PDF файлу
5. Frontend открывает файл

### Drag-and-Drop

**Реализация:**
- Используем `svelte-dnd-action`
- Блоки в Side Panel — draggable
- Canvas — drop zone
- При drop: создаём новый блок на позиции курсора
- Автоматическое выравнивание к сетке (snap to grid)

## 📋 Чеклист реализации

### Фаза 1: Canvas и базовая структура (День 1-2)
- [ ] Создать Canvas.svelte с реальной страницей A4
- [ ] Реализовать сетку и направляющие
- [ ] Добавить масштабирование и прокрутку
- [ ] Создать базовый Block.svelte компонент
- [ ] Настроить stores (blocks, selectedBlock, page)

### Фаза 2: Блоки (День 3-4)
- [ ] Text Block с inline редактированием
- [ ] Image Block с загрузкой и изменением размера
- [ ] Table Block (базовая таблица)
- [ ] Spacer Block
- [ ] Выделение блоков (рамка, ручки)

### Фаза 3: Управление (День 5-6)
- [ ] Drag-and-drop из Side Panel
- [ ] Перемещение блоков на canvas
- [ ] Изменение размера (drag ручки)
- [ ] Удаление блоков
- [ ] Панель свойств (PropertiesPanel)

### Фаза 4: Генерация (День 7-8)
- [ ] Python модуль для PDF генерации
- [ ] Tauri command для генерации
- [ ] Конвертация блоков в PDF
- [ ] Сохранение и открытие PDF
- [ ] Обработка ошибок

### Фаза 5: Сохранение (День 9-10)
- [ ] Сохранение документа в JSON
- [ ] Загрузка документа
- [ ] Автосохранение
- [ ] UI для управления документами

## 🎉 Результат

После первой реализации у нас будет:
- ✅ Работающий визуальный редактор с блоками
- ✅ Реальная страница A4 на холсте
- ✅ Drag-and-drop интерфейс
- ✅ Генерация идеального PDF
- ✅ Сохранение/загрузка документов

**Пользователь сможет:**
1. Открыть SimpleDoc
2. Увидеть чистую страницу A4
3. Перетащить блоки из панели на страницу
4. Отредактировать содержимое
5. Нажать "Generate PDF"
6. Получить профессиональный документ за секунды

## 🚀 Следующие шаги после v1.0

- Множественные страницы
- AI интеграция для генерации контента
- Расширенная библиотека блоков (графики, QR-коды)
- Шаблоны документов
- Условная логика и переменные
- DOCX и HTML экспорт

---

**Let's build something amazing! 🎨✨**

