# User Pipeline: Визуальный редактор документов

## 🎯 Уникальная концепция: Notion + Canva/Word

SimpleDoc сочетает **лучшее из двух миров**:

### ✨ Свобода Notion
- **Блочная архитектура** — каждый элемент = независимый блок
- **AI-генерация структуры** — создание документа по промпту
- **Гибкое редактирование** — drag-and-drop, копирование, группировка
- **Анимации для презентаций** — переходы между слайдами
- **Интуитивный интерфейс** — редактирование как в Notion

### 📐 Жёсткий Page Layout (Canva/Word)
- **Реальные страницы** — A4, Letter, Custom с точными размерами
- **Сетки и направляющие** — пиксель-идеальное позиционирование
- **Точный контроль позиций** — координаты, выравнивание, отступы
- **Идеальный PDF** — профессиональная печать без искажений
- **Множественные страницы** — управление как в Word/InDesign

### 💼 Killer Feature для бизнеса

Эта комбинация делает SimpleDoc **идеальным решением** для:
- **Коммерческие предложения** — профессиональный вид + быстрая генерация
- **Отчёты** — структурированные данные + точное форматирование
- **Контракты** — юридические документы с идеальным layout
- **Маркетинговые материалы** — креативный дизайн + готовность к печати

**Результат:** Документы, которые выглядят профессионально в печати И удобно редактируются.

## Обзор концепции

SimpleDoc как гибридный редактор документов, где каждый элемент представлен как независимый блок (как в Notion), но с жёстким контролем позиционирования и layout'а (как в Canva/Word):

**Блочная свобода:**
- Перемещать drag-and-drop
- Редактировать напрямую
- Копировать/дублировать
- Группировать
- Стилизовать независимо
- Анимировать (для презентаций)

**Точный layout:**
- Реальные размеры страниц (A4, Letter, Custom)
- Сетки и направляющие для выравнивания
- Точные координаты (X, Y, ширина, высота)
- Контроль отступов и полей
- Множественные страницы с нумерацией
- Разрывы страниц и колонки

## User Journey: Полный цикл работы с документом

### 1. Создание нового документа

**Сценарий A: С нуля**
```
Пользователь → "Новый документ" 
  → Выбор формата страницы:
    - A4 (210×297 мм) - стандарт для документов
    - Letter (8.5×11 дюймов) - для США
    - Legal, A3, A5, Custom
  → Выбор ориентации (Portrait/Landscape)
  → Настройка полей (верх, низ, лево, право)
  → Выбор сетки (сетка, направляющие, без сетки)
  → Создание первой страницы
  → Холст отображает реальную страницу с границами
  → Готов к добавлению блоков
```

**Важно:** Холст показывает **реальную страницу** с точными размерами, как в Canva/InDesign, а не бесконечный скролл как в Notion.

**Сценарий B: Из шаблона**
```
Пользователь → "Создать из шаблона"
  → Просмотр библиотеки шаблонов (галерея)
  → Выбор шаблона
  → Загрузка шаблона на холст
  → Все блоки становятся редактируемыми
  → Замена placeholder'ов данными
```

**Сценарий C: С AI-помощью**
```
Пользователь → "Создать с AI"
  → Ввод промпта: "Коммерческое предложение для IT-компании"
  → AI генерирует структуру документа
  → AI создает блоки: заголовок, описание услуг, прайс-лист, контакты
  → Пользователь редактирует/корректирует результат
```

### 2. Работа с блоками

#### 2.1 Добавление блоков

**Типы блоков:**
- **Текстовые блоки**
  - Заголовок (H1-H6)
  - Параграф
  - Список (маркированный/нумерованный)
  - Цитата
  - Код (для технических документов)

- **Медиа блоки**
  - Изображение (с drag-to-resize)
  - Видео (для интерактивных документов)
  - Иконки/SVG

- **Структурные блоки**
  - Таблица (редактируемая, как в Excel)
  - Колонки (2-3-4 колонки)
  - Разделитель
  - Spacer (отступ)

- **Интерактивные блоки**
  - Форма (для заполнения)
  - Кнопка (для PDF с действиями)
  - Чекбокс/Радио

- **Специализированные блоки**
  - График/Диаграмма (Chart.js интеграция)
  - QR-код
  - Штрих-код
  - Подпись (для договоров)
  - Печать/Штамп

- **AI-блоки**
  - AI Text Generator (генерирует текст по промпту)
  - AI Image Generator (генерирует изображения)
  - AI Data Analyzer (анализирует данные и создает графики)

**Процесс добавления:**
```
Пользователь → Панель инструментов слева
  → Выбор типа блока
  → Клик на холст (блок появляется)
  ИЛИ
  → Drag-and-drop на холст
  → Блок автоматически позиционируется
  → Блок сразу в режиме редактирования
```

#### 2.2 Редактирование блоков

**Текстовые блоки:**
```
Клик на блок → Активация inline-редактора
  → WYSIWYG редактирование (Bold, Italic, Color, Size)
  → Панель форматирования появляется при выделении
  → Редактирование в реальном времени
  → Автосохранение изменений
```

**Изображения:**
```
Клик на изображение → Выделение с ручками изменения размера
  → Drag углов для изменения размера (с сохранением пропорций)
  → Drag сторон для искажения
  → Двойной клик → Замена изображения
  → Правый клик → Фильтры, обрезка, настройки
```

**Таблицы:**
```
Клик на таблицу → Редактируемая таблица
  → Добавление/удаление строк/столбцов
  → Форматирование ячеек
  → Формулы (как в Excel)
  → Стилизация (цвета, границы)
```

#### 2.3 Управление блоками

**Выделение:**
- Одиночный клик → Выделение одного блока
- Ctrl+Click → Множественное выделение
- Drag → Выделение области (все блоки внутри)
- Ctrl+A → Выделить все

**Перемещение:**
- Drag выделенного блока → Перемещение
- Стрелки клавиатуры → Точное перемещение (1px)
- Shift+Стрелки → Крупное перемещение (10px)
- Snap to grid → Автоматическое выравнивание

**Изменение размера:**
- Drag ручек → Изменение размера
- Shift+Drag → Сохранение пропорций
- Alt+Drag → Изменение от центра
- Поле ввода размеров → Точные значения

**Группировка:**
```
Выделить несколько блоков → Ctrl+G
  → Блоки объединяются в группу
  → Группа ведет себя как один блок
  → Можно разгруппировать (Ctrl+Shift+G)
```

**Копирование:**
- Ctrl+C / Ctrl+V → Копирование/вставка
- Ctrl+D → Дублирование
- Drag с Alt → Копирование при перетаскивании

**Удаление:**
- Delete / Backspace → Удаление выделенных блоков
- Undo/Redo → Отмена/повтор действий

### 2.4 Управление страницами (Page Layout)

**Ключевая особенность:** SimpleDoc работает с **реальными страницами**, как в Word/InDesign, а не с бесконечным скроллом как в Notion.

#### Добавление страниц
```
Пользователь → Кнопка "Добавить страницу"
  → Новая страница создаётся после текущей
  → Автоматическая нумерация
  → Можно дублировать текущую страницу
  → Можно вставить пустую страницу между существующими
```

#### Навигация по страницам
```
Панель миниатюр страниц (слева или снизу):
  - Миниатюры всех страниц
  - Клик → переход на страницу
  - Drag → изменение порядка страниц
  - Правый клик → удаление, дублирование
```

#### Разрывы страниц
```
Блок → Свойства → "Разрыв страницы"
  → Блок принудительно переносится на новую страницу
  → Или автоматический разрыв при переполнении
  → Настройка: "Не разрывать блок" (заголовок с текстом)
```

#### Колонки и макет
```
Страница → Свойства → "Колонки"
  - 1 колонка (стандарт)
  - 2 колонки (газетный стиль)
  - 3 колонки
  - Custom (неравномерные колонки)
  → Блоки автоматически размещаются в колонках
```

#### Сетки и направляющие
```
Вид → "Показать сетку"
  → Сетка помогает выравнивать блоки
  → Настраиваемый шаг сетки (5px, 10px, 20px)
  → Snap to grid (автоматическое выравнивание)

Вид → "Направляющие"
  - Вертикальные направляющие (для центрирования)
  - Горизонтальные направляющие (для выравнивания)
  - Маргины (поля страницы)
  - Drag для создания кастомных направляющих
```

#### Точное позиционирование
```
Блок → Свойства → "Позиция"
  - X, Y координаты (в пикселях или мм)
  - Ширина, высота (точные значения)
  - Выравнивание относительно страницы:
    * По центру
    * По левому/правому краю
    * По верхнему/нижнему краю
  - Отступы от краёв страницы
```

#### Предпросмотр печати
```
Вид → "Предпросмотр печати"
  → Отображение всех страниц как они будут напечатаны
  → Показ разрывов страниц
  → Проверка полей и отступов
  → Масштабирование для просмотра
```

### 3. Стилизация и дизайн

#### 3.1 Панель свойств (справа)

При выделении блока появляется панель свойств:

**Для текстовых блоков:**
- Шрифт (семейство, размер, вес)
- Цвет текста
- Выравнивание
- Межстрочный интервал
- Отступы
- Тени, обводка

**Для изображений:**
- Прозрачность
- Фильтры (яркость, контраст, насыщенность)
- Обрезка
- Скругление углов
- Обводка

**Для всех блоков:**
- Фон (цвет, градиент, изображение)
- Отступы (padding)
- Границы (border)
- Тени
- Позиция (X, Y, Z-index)
- Размер (ширина, высота)
- Поворот

#### 3.2 Темы и стили

```
Пользователь → Панель "Темы"
  → Выбор готовой темы (Corporate, Modern, Classic)
  → Применение темы ко всему документу
  → ИЛИ создание своей темы
  → Сохранение темы в библиотеку
```

**Стили блоков:**
- Предустановленные стили (Primary Button, Heading 1, etc.)
- Сохранение своих стилей
- Применение стиля к блоку одним кликом

### 4. Работа с данными и переменными

#### 4.1 Динамические данные

**Placeholder'ы в блоках:**
```
Пользователь → Ввод текста в блок
  → Ввод {{company_name}}
  → Система распознает как переменную
  → Подсветка переменной (цветом)
  → При генерации заменяется на реальное значение
```

**Панель данных (слева, вкладка "Данные"):**
```
Структура данных:
- Клиенты
  - Имя
  - Компания
  - Email
  - Телефон
- Продукты
  - Название
  - Цена
  - Описание
- Настройки
  - Дата
  - Номер документа
  - Валюта
```

**Привязка данных:**
```
Выделить блок → Панель свойств
  → Поле "Данные" → Выбор источника
  → Выбор поля из источника
  → Блок автоматически заполняется
  → При изменении данных → Блок обновляется
```

#### 4.2 Условная логика

```
Блок → Свойства → Условия
  → "Показывать если: {{amount}} > 1000"
  → Блок скрывается/показывается в зависимости от данных
```

#### 4.3 Повторы (Repeater)

```
Блок → Свойства → "Повторять для каждого"
  → Выбор массива данных (например, список товаров)
  → Блок дублируется для каждого элемента
  → Внутри блока доступны переменные элемента
```

### 5. AI-интеграция

#### 5.1 Генерация контента

**AI Text Generator:**
```
Добавить блок "AI Text"
  → Ввод промпта: "Напиши описание IT-услуг для малого бизнеса"
  → AI генерирует текст
  → Текст появляется в блоке
  → Пользователь редактирует/принимает
  → Можно регенерировать с другими параметрами
```

**AI Image Generator:**
```
Добавить блок "AI Image"
  → Ввод промпта: "Профессиональное изображение офиса"
  → AI генерирует изображение
  → Изображение появляется в блоке
  → Можно регенерировать, изменить стиль
```

**AI Data Analyzer:**
```
Выделить таблицу с данными
  → Кнопка "AI Анализ"
  → AI анализирует данные
  → Предлагает создать график/диаграмму
  → Автоматически создает визуализацию
```

#### 5.2 Улучшение контента

```
Выделить текстовый блок
  → Кнопка "Улучшить с AI"
  → AI предлагает улучшения:
    - Исправление грамматики
    - Улучшение стиля
    - Расширение текста
    - Сокращение текста
  → Пользователь выбирает вариант
```

#### 5.3 Перевод

```
Выделить блок(и)
  → Кнопка "Перевести"
  → Выбор языка
  → AI переводит контент
  → Заменяет оригинал или создает копию
```

### 6. Предпросмотр и генерация

#### 6.1 Live Preview

```
Холст → Режим "Предпросмотр"
  → Документ отображается как финальный результат
  → Блоки не редактируемые
  → Можно прокручивать, масштабировать
  → Переключение между режимами редактирования/предпросмотра
```

#### 6.2 Генерация документа

**Процесс:**
```
Пользователь → Кнопка "Сгенерировать"
  → Выбор формата:
    - PDF (рекомендуется) - идеальная печать, точное позиционирование
    - DOCX - для дальнейшего редактирования в Word
    - HTML - для веб-версий
    - PPTX - для презентаций (с анимациями)
  → Заполнение данных (если есть переменные)
  → Обработка:
    1. Замена всех переменных на данные
    2. Применение стилей (шрифты, цвета, эффекты)
    3. Точное позиционирование блоков (X, Y координаты)
    4. Разбивка на страницы (с учётом разрывов)
    5. Применение полей и отступов
    6. Конвертация в выбранный формат
  → Показ прогресса
  → Открытие сгенерированного файла
  → Сохранение в выбранную папку
```

**Идеальный PDF (Killer Feature):**
```
PDF генерация использует reportlab с полным контролем:
  - Точные координаты каждого блока (в пикселях/точках)
  - Реальные размеры страниц (A4 = 210×297 мм)
  - Профессиональная печать без искажений
  - Сохранение всех стилей (тени, градиенты, прозрачность)
  - Векторная графика (масштабирование без потери качества)
  - Поддержка CMYK для профессиональной печати
  - Метаданные (автор, название, ключевые слова)
```

**DOCX генерация:**
```
Для простых документов:
  - Использование таблиц для layout'а
  - Text boxes для позиционирования
  - Сохранение базовых стилей
  - Совместимость с Word

Для сложных макетов:
  - Рекомендуется использовать PDF
  - DOCX имеет ограничения по позиционированию
```

**Batch Generation:**
```
Пользователь → "Пакетная генерация"
  → Выбор шаблона документа
  → Загрузка CSV/Excel с данными
  → Каждая строка = один документ
  → Генерация всех документов
  → Сохранение в папку
```

### 7. Сохранение и шаблоны

#### 7.1 Сохранение документа

```
Автосохранение:
  - Каждые 30 секунд (настраиваемо)
  - При закрытии блока
  - При изменении данных

Ручное сохранение:
  - Ctrl+S → Сохранить
  - Ctrl+Shift+S → Сохранить как...
  - Выбор имени, папки
```

#### 7.2 Сохранение как шаблона

```
Пользователь → "Сохранить как шаблон"
  → Ввод названия, описания
  → Выбор категории
  → Определение переменных (какие блоки будут заменяться)
  → Сохранение в библиотеку шаблонов
  → Шаблон доступен для использования
```

#### 7.3 Библиотека шаблонов

```
Пользователь → "Шаблоны"
  → Просмотр всех шаблонов
  → Фильтрация по категориям
  → Поиск
  → Предпросмотр
  → Использование шаблона
  → Редактирование шаблона
  → Удаление шаблона
```

### 8. Коллаборация (будущее)

```
Пользователь → "Поделиться"
  → Выбор пользователей/команд
  → Установка прав (чтение/редактирование)
  → Отправка ссылки
  → Редактирование в реальном времени
  → История изменений
  → Комментарии к блокам
```

## Техническая реализация: Оценка возможностей

### ✅ Что позволяет текущий стек

**Svelte + Tauri:**
- ✅ Отличная производительность для drag-and-drop
- ✅ Нативная работа с файловой системой
- ✅ Кроссплатформенность
- ✅ Малый размер бандла
- ✅ Доступ к системным API

**Возможности для блочного редактора:**
- ✅ Svelte отлично подходит для компонентной архитектуры (каждый блок = компонент)
- ✅ Реактивность Svelte идеальна для live preview
- ✅ Можно использовать библиотеки типа `svelte-dnd-action` для drag-and-drop
- ✅ Canvas API для отрисовки (если нужен кастомный рендеринг)

**Python для генерации:**
- ✅ `python-docx` позволяет создавать сложные документы программно
- ✅ Можно позиционировать элементы (хотя ограниченно)
- ✅ Поддержка стилей, форматирования
- ✅ Генерация PDF с `reportlab` (полный контроль над позиционированием)

### ⚠️ Ограничения и решения

**Ограничение 1: Позиционирование в DOCX**
- **Проблема:** DOCX не поддерживает абсолютное позиционирование как в Canva
- **Решение:** 
  - Использовать таблицы для layout'а
  - Использовать text boxes с позиционированием
  - Для сложных макетов → генерировать PDF (полный контроль)
  - Для простых → DOCX с таблицами

**Ограничение 2: WYSIWYG редактирование**
- **Проблема:** Нужен полноценный rich text editor
- **Решение:**
  - Использовать `contenteditable` с кастомной обработкой
  - Или библиотеку типа `Quill.js` / `Tiptap` (есть Svelte версии)
  - Или `ProseMirror` (самый мощный, но сложнее)

**Ограничение 3: Сложные визуальные эффекты**
- **Проблема:** Тени, градиенты, фильтры в DOCX ограничены
- **Решение:**
  - Для PDF → полная поддержка через `reportlab`
  - Для DOCX → базовые эффекты, сложные → конвертация в изображения

**Ограничение 4: Интерактивность**
- **Проблема:** DOCX не поддерживает интерактивные элементы
- **Решение:**
  - PDF поддерживает формы, кнопки
  - HTML экспорт для веб-версий
  - PPTX для презентаций

### 🚀 Рекомендуемый стек для блочного редактора

**Frontend (Svelte):**
```javascript
// Основные библиотеки
- svelte-dnd-action // Drag and drop
- tiptap или prosemirror // Rich text editing
- fabric.js или konva.js // Canvas для сложной графики (опционально)
- chart.js // Графики и диаграммы
- html2canvas // Экспорт в изображения
```

**Backend (Rust/Tauri):**
```rust
// Команды для:
- Управления блоками (CRUD)
- Сохранения/загрузки документов
- Интеграции с Python
- Работы с файловой системой
```

**Python генерация:**
```python
// Модули:
- python-docx // DOCX генерация
- reportlab // PDF генерация (полный контроль)
- python-pptx // PPTX генерация
- Pillow // Обработка изображений
- jinja2 // Шаблонизация (для простых случаев)
```

### 📋 План реализации блочного редактора

**Фаза 1: Базовая структура блоков**
1. Создать систему блоков (Block component)
2. Реализовать drag-and-drop
3. Базовое позиционирование
4. Выделение блоков
5. Панель свойств

**Фаза 2: Типы блоков**
1. Текстовые блоки (с rich text editor)
2. Изображения
3. Таблицы
4. Разделители, отступы

**Фаза 3: Стилизация**
1. Панель свойств для каждого типа
2. Темы
3. Предустановленные стили

**Фаза 4: Данные и переменные**
1. Система переменных
2. Привязка данных
3. Условная логика
4. Повторы

**Фаза 5: Генерация**
1. Конвертация блоков в DOCX
2. Конвертация блоков в PDF
3. Сохранение позиционирования
4. Применение стилей

**Фаза 6: AI интеграция**
1. API интеграция (OpenAI, Anthropic, etc.)
2. AI блоки
3. Генерация контента
4. Улучшение текста

**Фаза 7: Продвинутые функции**
1. Группировка
2. Слои (z-index)
3. Сетки и направляющие
4. Snap to grid
5. История изменений (undo/redo)

## Вывод: Можно ли реализовать Canva-like редактор?

### ✅ ДА, но с нюансами:

**Что получится отлично:**
- ✅ Визуальный редактор с блоками
- ✅ Drag-and-drop интерфейс
- ✅ Редактирование в реальном времени
- ✅ Стилизация и дизайн
- ✅ AI интеграция
- ✅ Генерация PDF (полный контроль)
- ✅ Генерация HTML (для веб)

**Что будет ограничено:**
- ⚠️ DOCX не поддерживает абсолютное позиционирование как в Canva
  - **Решение:** Использовать PDF для сложных макетов, DOCX для простых
- ⚠️ Сложные визуальные эффекты в DOCX
  - **Решение:** PDF поддерживает все, DOCX - базовые эффекты

**Рекомендация:**
1. **Начать с PDF-ориентированного редактора** - полный контроль над дизайном
2. **DOCX как дополнительный экспорт** - для простых документов
3. **HTML экспорт** - для веб-версий и интерактивности
4. **PPTX экспорт** - для презентаций

**Итог:** Проект **абсолютно реализуем** и будет **лучше Word** для компаний, потому что:
- Визуальный редактор вместо текстового
- Блочная архитектура (легче работать)
- AI интеграция
- Быстрая генерация
- Шаблоны и библиотеки
- Профессиональный дизайн

Текущий стек (Svelte + Tauri + Python) **идеально подходит** для этой задачи!

