# Frontend Architecture - SimpleDoc

## Структура проекта

```
src/
├── lib/
│   ├── components/
│   │   ├── ui/              # Базовые UI компоненты
│   │   │   ├── Button.svelte
│   │   │   ├── Input.svelte
│   │   │   ├── Card.svelte
│   │   │   ├── LoadingSpinner.svelte
│   │   │   └── Notification.svelte
│   │   │
│   │   ├── layout/          # Компоненты layout
│   │   │   ├── AppLayout.svelte
│   │   │   ├── AppHeader.svelte
│   │   │   ├── Ribbon.svelte
│   │   │   └── Sidebar.svelte
│   │   │
│   │   └── features/        # Feature-specific компоненты
│   │       └── DocumentEditor.svelte
│   │
│   ├── stores/              # Svelte stores для state management
│   │   └── app.ts
│   │
│   ├── types/               # TypeScript типы
│   │   └── index.ts
│   │
│   ├── utils/               # Утилиты
│   │   └── index.ts
│   │
│   └── constants/           # Константы
│       └── ribbon.ts
│
├── routes/                  # SvelteKit routes
│   ├── +layout.svelte
│   └── +page.svelte
│
└── styles/                  # Глобальные стили
    └── app.css
```

## Дизайн система

### Цвета (MS Office-inspired)

- **Primary**: Office Blue (#217346) - основной акцентный цвет
- **Ribbon BG**: #f3f3f3 - фон ленты
- **Borders**: #d4d4d4 - границы элементов
- **Hover**: #e5e5e5 - состояние наведения

### Компоненты

#### Button
- Варианты: `default`, `primary`, `ghost`
- Размеры: `sm`, `md`, `lg`
- Поддержка `large` для больших кнопок в Ribbon

#### Input
- Стиль в духе Office
- Поддержка label и error states
- Адаптивная валидация

#### Ribbon
- Система вкладок с группами кнопок
- Возможность сворачивания
- Динамическая конфигурация через `ribbonConfig`

## State Management

Используются Svelte stores:

- `settings` - настройки приложения
- `documents` - список документов
- `currentDocument` - текущий открытый документ
- `templates` - шаблоны
- `clients` - клиенты
- `isRibbonCollapsed` - состояние Ribbon
- `activeRibbonTab` - активная вкладка Ribbon
- `sidebarOpen` - состояние боковой панели
- `notification` - уведомления

## Масштабируемость

### Добавление новых компонентов UI

1. Создайте компонент в `src/lib/components/ui/`
2. Используйте TailwindCSS классы из `app.css`
3. Экспортируйте пропсы через TypeScript интерфейсы

### Добавление новых features

1. Создайте компоненты в `src/lib/components/features/`
2. Добавьте необходимые stores в `src/lib/stores/`
3. Добавьте типы в `src/lib/types/`
4. Интегрируйте в routes

### Расширение Ribbon

Редактируйте `src/lib/constants/ribbon.ts`:

```typescript
{
  id: 'NEW_TAB',
  label: 'Новая вкладка',
  groups: [
    {
      id: 'new-group',
      label: 'Новая группа',
      items: [
        {
          id: 'new-item',
          label: 'Новый элемент',
          icon: '🔧',
          action: async () => {
            // Ваша логика
          },
        },
      ],
    },
  ],
}
```

## Стилизация

Используется TailwindCSS с кастомной конфигурацией:
- Кастомные цвета Office
- Кастомные компоненты (`.btn-office`, `.input-office`, `.panel-office`)
- Smooth transitions через `.transition-office`

## Горячие клавиши

Поддержка через атрибут `shortcut` в Ribbon items. Можно расширить через глобальный handler.

## Темная тема

Готова инфраструктура (darkMode в tailwind.config), можно активировать через store `settings.theme`.

